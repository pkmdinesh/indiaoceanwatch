name: Update coastal advisory status

on:
  schedule:
    # Retry twice an hour because GitHub schedules are best-effort. The script
    # itself suppresses scheduled updates until the saved snapshot is 6h old.
    - cron: "17,47 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: coastal-status-update
  cancel-in-progress: false

jobs:
  update:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Read official INCOIS sources
        shell: powershell
        run: |
          if ("${{ github.event_name }}" -eq "schedule") {
            .\update-status.ps1 -Quiet -MinimumAgeHours 6
          } else {
            .\update-status.ps1 -Quiet
          }
      - name: Commit updated snapshot
        shell: powershell
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add status.json
          git diff --cached --quiet
          if ($LASTEXITCODE -ne 0) {
            git commit -m "Update coastal advisory status"
            git push
          }
